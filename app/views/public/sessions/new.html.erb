<div class="mt-4 ml-5 mb-5">
  <h4 class="ml-5">
    <span class="bg-light ml-5">ログイン</span>
  </h4>
</div>
<div class="container">
  <div class="col-2 ml-5">
    <span class="bg-light ml-5">会員の方はこちらからログイン</span>
  </div>
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

  <div>
    <%= f.label :email, "メールアドレス" %>
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>
  <div class="row justify-content-center mb-3">
    <div class="col-2">
      <%= f.label :メールアドレス %>
    </div>
    <div class="col-4">
      <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "sample@example.com", class:"w-100" %>
    </div>
  </div>
  <div class="field">
    <%= f.label :password, "パスワード" %>
    <%= f.password_field :password, autocomplete: "current-password" %>
  </div>

  <div class="actions">
    <%= f.submit "ログイン" %>
  </div>
<% end %>

<p>会員登録がお済み出ない方</p>
<%= render "public/shared/links" %>から新規登録を行ってください。
